<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="fill_parent"
    android:layout_height="fill_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@drawable/sanidad_publica"
    tools:context=".Principal_Activity">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="250dp"
        android:layout_height="100dp"
        android:weightSum="1"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true">
        <Button
            android:id="@+id/buttonIdentificarPrincipal"
            android:layout_width="195dp"
            android:layout_height="wrap_content"
            android:text="Identificar"
            android:onClick="signIn"
            android:drawableLeft="@drawable/ic_action_accounts"
            android:layout_weight="0.04"
            android:layout_gravity="bottom|right" />

        <Button
            android:id="@+id/buttonRegistrarPrincipal"
            android:layout_width="195dp"
            android:layout_height="wrap_content"
            android:text="Registrar"
            android:layout_gravity="bottom|right"
            android:layout_weight="0.04"
            android:drawableLeft="@drawable/ic_action_add_person" />
    </LinearLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Recordatorio Tratamiento"
        android:id="@+id/textView7"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:textSize="30dp"
        android:textStyle="bold" />
</RelativeLayout>
